import styled from 'styled-components';
import AntdInputNumber from 'antd/lib/input-number/index';
import { Color } from '../ui';

export const InputNumber = styled(AntdInputNumber)`
  border: 1px solid ${Color.lightGray};
`;
